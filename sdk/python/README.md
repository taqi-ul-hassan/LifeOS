# LifeOS Python SDK scaffold

Use the versioned REST API with bearer tokens. This scaffold intentionally contains no credentials or generated client code; generate it from the backend OpenAPI schema as part of the public-release CI pipeline.
